[project]
name = "modern-mind-kb-tools"
version = "0.3.0"
description = "On-demand CLI tools for the Modern Mind Knowledge Base (search, lint, index)"
requires-python = ">=3.13"
license = "MIT"
dependencies = [
    "pyyaml>=6.0",
]
